version: "3.9"
services:
  node1:
    build:
      context: ./nodes/
      dockerfile: Dockerfile
    networks:
      - paxos_backend
    ports:
      - 8001:8000

  node2:
    build:
      context: ./nodes/
      dockerfile: Dockerfile
    networks:
      - paxos_backend
    ports:
      - 8002:8000

  node3:
    build:
      context: ./nodes/
      dockerfile: Dockerfile
    networks:
      - paxos_backend
    ports:
      - 8003:8000

networks:
  paxos_backend:
